{% extends 'base.html.twig' %}

{% block title %}Edit Ad{% endblock %}

{% block body %}
<div class="container">
    <div class="w-100 d-flex mx-auto mt-3">
        <h1 class="mt-3 mx-auto">Modifier {{ ad.title }}</h1>
    </div>

    {{ include('ad/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="mb-4 btn btn-warning text-light" href="{{ path('ad_index') }}">Retour</a>

    {{ include('ad/_delete_form.html.twig') }}
</div>
{% endblock %}
