{% extends 'base.html.twig' %}

{% block title %}Modifier la catégorie{% endblock %}

{% block body %}
<div class="container">
    <div class="w-100 d-flex mx-auto mt-3">
        <h1 class="mt-3 mx-auto">Modifier la catégorie</h1>
    </div>

    {{ include('admin/category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="mb-4 btn btn-warning text-light" href="{{ path('category_index') }}">Retour</a>

    {{ include('admin/category/_delete_form.html.twig') }}
</div>
{% endblock %}
